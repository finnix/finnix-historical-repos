#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

override_dh_auto_install:
	install -d -m 0755 $(CURDIR)/debian/grub-finnix/etc/grub.d
	install -m 0755 debian/grub.d $(CURDIR)/debian/grub-finnix/etc/grub.d/20_finnix
